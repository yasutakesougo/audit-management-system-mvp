#############################################
# Example Environment Configuration
# Copy to .env and adjust values.
#############################################

# Required SharePoint settings
# Root tenant SharePoint URL (no trailing slash)
VITE_SP_RESOURCE=https://<yourtenant>.sharepoint.com

# Site relative path (leading slash required, no trailing slash)
VITE_SP_SITE_RELATIVE=/sites/<SiteName>

# Optional debug flag (1 enables verbose logging for audit & spClient)
# VITE_AUDIT_DEBUG=1

# MSAL (Azure AD) authentication
VITE_MSAL_CLIENT_ID=__FILL_ME__
VITE_MSAL_TENANT_ID=__FILL_ME__
# Optional overrides for advanced scenarios
# VITE_MSAL_AUTHORITY=https://login.microsoftonline.com/<tenant-guid>
# VITE_MSAL_REDIRECT_URI=https://localhost:5173

# Optional retry tuning
# VITE_SP_RETRY_MAX=4
# VITE_SP_RETRY_BASE_MS=400
# VITE_SP_RETRY_MAX_DELAY_MS=5000

# Remove all placeholders (<yourtenant>, <SiteName>, __FILL_ME__) before running.
