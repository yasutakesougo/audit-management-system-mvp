name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main, develop]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  typecheck-and-test:
    name: TypeCheck & Test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      
      - name: Install dependencies
        run: npm ci
      
      - name: TypeScript type check
        id: typecheck
        run: |
          echo "Running TypeScript type check..."
          npm run typecheck
          echo "✅ TypeScript type check completed successfully"
      
      - name: Run tests
        id: tests
        run: |
          echo "Running unit tests..."
          npm run test:ci
          echo "✅ Tests completed successfully"
      
      - name: Summary
        if: always()
        run: |
          echo "## CI Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- TypeScript typecheck: ${{ steps.typecheck.outcome || 'not run' }}" >> $GITHUB_STEP_SUMMARY
          echo "- Tests: ${{ steps.tests.outcome || 'not run' }}" >> $GITHUB_STEP_SUMMARY
