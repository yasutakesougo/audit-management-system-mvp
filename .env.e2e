# E2E Test Environment Variables for Playwright
# These are defaults for local development. CI workflows can override these.

# E2E mode flags
VITE_E2E=1
VITE_DEMO_MODE=1
VITE_SKIP_LOGIN=1
VITE_E2E_MSAL_MOCK=1
VITE_SKIP_SHAREPOINT=1

# Dummy MSAL credentials for E2E tests
VITE_MSAL_CLIENT_ID=dummy
VITE_MSAL_TENANT_ID=dummy

# SharePoint test configuration
VITE_SP_RESOURCE=https://contoso.sharepoint.com
VITE_SP_SITE_RELATIVE=/sites/Audit
VITE_SP_SCOPE_DEFAULT=https://contoso.sharepoint.com/AllSites.Read

# Feature flags
VITE_FEATURE_SCHEDULES=1

# Timezone for tests
# Set to Asia/Tokyo to match the CI default (see .github/workflows/test.yml)
# This ensures consistent date/time behavior between local and CI test runs
VITE_SCHEDULES_TZ=Asia/Tokyo