# 📋 Nightly Issue Drafts — 2026-03-31

> Generated by nightly-patrol Phase B — 2026-03-31

## サマリー

| # | Severity | Category | Title |
|:-:|:--------:|----------|-------|
| 1 | 🔴 critical | large-file | [refactor] src/features/daily/repositories/sharepoint/SharePointDailyRecordRepository.ts (1109行) を分割 |
| 2 | 🔴 critical | large-file | [refactor] src/features/users/infra/RestApiUserRepository.ts (874行) を分割 |
| 3 | 🔴 critical | large-file | [refactor] src/pages/TodayOpsPage.tsx (825行) を分割 |
| 4 | 🔴 critical | large-file | [refactor] src/debug/SpDevPanel.tsx (801行) を分割 |
| 5 | 🟠 high | large-file | [refactor] 巨大ファイル 7 件を監視・段階分割 |
| 6 | 🟠 high | type-safety | [type-safety] any 使用 22 件を型安全化 |

**合計:** 6 件
(🔴 critical: 4 / 🟠 high: 2)

---

## 1. 🔴 [critical] [refactor] src/features/daily/repositories/sharepoint/SharePointDailyRecordRepository.ts (1109行) を分割

### 背景

Thin Orchestrator パターンでは 1 ファイル ≤200行を推奨。
800行超はレビュー困難・テスト困難・変更事故のリスクが急上昇。
nightly patrol で検知。

### 現状

`src/features/daily/repositories/sharepoint/SharePointDailyRecordRepository.ts` が 1109 行で、即分割の閾値 (800行) を超えています。

### 対象ファイル

- `src/features/daily/repositories/sharepoint/SharePointDailyRecordRepository.ts`

### 提案

- 表示層 (Presentational) と導出ロジック (Hook) を分離
- 純関数を domain/ 配下へ移動
- Orchestrator を ≤200行 に維持
- `/refactor` ワークフローで段階的に分割

### 受入条件

- [ ] `src/features/daily/repositories/sharepoint/SharePointDailyRecordRepository.ts` が 400 行未満になること
- [ ] 既存テストがすべて通ること (`npx vitest run`)
- [ ] 型チェックが通ること (`npx tsc --noEmit`)
- [ ] 振る舞いが既存と一致すること

**Labels:** `refactor`, `nightly-patrol`, `tech-debt`, `priority-high`

---

## 2. 🔴 [critical] [refactor] src/features/users/infra/RestApiUserRepository.ts (874行) を分割

### 背景

Thin Orchestrator パターンでは 1 ファイル ≤200行を推奨。
800行超はレビュー困難・テスト困難・変更事故のリスクが急上昇。
nightly patrol で検知。

### 現状

`src/features/users/infra/RestApiUserRepository.ts` が 874 行で、即分割の閾値 (800行) を超えています。

### 対象ファイル

- `src/features/users/infra/RestApiUserRepository.ts`

### 提案

- 表示層 (Presentational) と導出ロジック (Hook) を分離
- 純関数を domain/ 配下へ移動
- Orchestrator を ≤200行 に維持
- `/refactor` ワークフローで段階的に分割

### 受入条件

- [ ] `src/features/users/infra/RestApiUserRepository.ts` が 400 行未満になること
- [ ] 既存テストがすべて通ること (`npx vitest run`)
- [ ] 型チェックが通ること (`npx tsc --noEmit`)
- [ ] 振る舞いが既存と一致すること

**Labels:** `refactor`, `nightly-patrol`, `tech-debt`, `priority-high`

---

## 3. 🔴 [critical] [refactor] src/pages/TodayOpsPage.tsx (825行) を分割

### 背景

Thin Orchestrator パターンでは 1 ファイル ≤200行を推奨。
800行超はレビュー困難・テスト困難・変更事故のリスクが急上昇。
nightly patrol で検知。

### 現状

`src/pages/TodayOpsPage.tsx` が 825 行で、即分割の閾値 (800行) を超えています。

### 対象ファイル

- `src/pages/TodayOpsPage.tsx`

### 提案

- 表示層 (Presentational) と導出ロジック (Hook) を分離
- 純関数を domain/ 配下へ移動
- Orchestrator を ≤200行 に維持
- `/refactor` ワークフローで段階的に分割

### 受入条件

- [ ] `src/pages/TodayOpsPage.tsx` が 400 行未満になること
- [ ] 既存テストがすべて通ること (`npx vitest run`)
- [ ] 型チェックが通ること (`npx tsc --noEmit`)
- [ ] 振る舞いが既存と一致すること

**Labels:** `refactor`, `nightly-patrol`, `tech-debt`, `priority-high`

---

## 4. 🔴 [critical] [refactor] src/debug/SpDevPanel.tsx (801行) を分割

### 背景

Thin Orchestrator パターンでは 1 ファイル ≤200行を推奨。
800行超はレビュー困難・テスト困難・変更事故のリスクが急上昇。
nightly patrol で検知。

### 現状

`src/debug/SpDevPanel.tsx` が 801 行で、即分割の閾値 (800行) を超えています。

### 対象ファイル

- `src/debug/SpDevPanel.tsx`

### 提案

- 表示層 (Presentational) と導出ロジック (Hook) を分離
- 純関数を domain/ 配下へ移動
- Orchestrator を ≤200行 に維持
- `/refactor` ワークフローで段階的に分割

### 受入条件

- [ ] `src/debug/SpDevPanel.tsx` が 400 行未満になること
- [ ] 既存テストがすべて通ること (`npx vitest run`)
- [ ] 型チェックが通ること (`npx tsc --noEmit`)
- [ ] 振る舞いが既存と一致すること

**Labels:** `refactor`, `nightly-patrol`, `tech-debt`, `priority-high`

---

## 5. 🟠 [high] [refactor] 巨大ファイル 7 件を監視・段階分割

### 背景

個別には即分割レベルではないが、件数が積み上がっている。
次回の変更時に分割を検討するべき候補。
nightly patrol で検知。

### 現状

600行以上のファイルが 7 件あり、監視閾値 (3件) を超えています。

### 対象ファイル

- `src/features/transport-assignments/domain/transportAssignmentDraft.ts`
- `src/features/today/transport/TransportStatusCard.tsx`
- `src/app/hubs/hubDefinitions.ts`
- `src/features/daily/infra/Legacy/SharePointDailyRecordRepository.ts`
- `src/features/users/infra/DataProviderUserRepository.ts`
- `src/debug/HydrationHud.tsx`
- `src/features/planning-sheet/tokuseiBridgeBuilders.ts`

### 提案

- 次回変更が入るタイミングで、対象ファイルの hook 抽出を検討
- 800行を超える前に予防的に分割
- `/refactor` ワークフローで段階的に対応

### 受入条件

- [ ] 各ファイルが次回変更時に 500 行未満を目標にすること
- [ ] 既存テストがすべて通ること

**Labels:** `refactor`, `nightly-patrol`, `tech-debt`, `priority-high`

---

## 6. 🟠 [high] [type-safety] any 使用 22 件を型安全化

### 背景

`any` はコンパイル時の型チェックを無効化し、本番障害のリスクを高める。
福祉現場OSとして、データ整合性は最重要。型安全は品質の基盤。
6 ファイルに分散（上位10件を表示）。
nightly patrol で検知。

### 現状

ソースコード内の `any` 使用が 22 件あり、閾値 (10件) を超えています。

### 対象ファイル

- `src/features/support-plan-guide/infra/DataProviderSupportPlanDraftRepository.ts`
- `src/features/schedules/infra/DataProviderScheduleRepository.ts`
- `src/features/monitoring/data/DataProviderMonitoringMeetingRepository.ts`
- `src/features/users/infra/DataProviderUserRepository.ts`
- `src/features/attendance/infra/Legacy/attendanceDailyRepository.ts`
- `src/features/users/utils/migrateUserSplitData.ts`

### 提案

- 上位10ファイルを `/review` ワークフローで型安全化
- `as any` → 適切な型ガード or generics に置き換え
- `: any` → Zod schema から推論した型に置き換え
- eslint `@typescript-eslint/no-explicit-any` ルールの段階的有効化を検討

### 受入条件

- [ ] any 使用が 15 件以下になること
- [ ] 型チェックが通ること (`npx tsc --noEmit`)
- [ ] 既存テストがすべて通ること

**Labels:** `type-safety`, `nightly-patrol`, `tech-debt`, `priority-high`

---

## 使い方

1. 上記の Draft を確認し、対応が必要なものを選ぶ
2. GitHub Issue として起票する（タイトル・本文をコピー）
3. 対応するワークフロー (`/refactor`, `/review`, `/test-design`) を実行

> ⚠️ この Draft は自動生成です。内容を確認してから起票してください。

---

*Generated by Nightly Patrol (Phase B) — Issue Draft Generator*