# 📋 Nightly Issue Drafts — 2026-03-22

> Generated by nightly-patrol Phase B — 2026-03-22

## サマリー

| # | Severity | Category | Title |
|:-:|:--------:|----------|-------|
| 1 | 🔴 critical | test-coverage | [test] テスト未整備 14 feature のテスト追加 |
| 2 | 🟠 high | large-file | [refactor] 巨大ファイル 6 件を監視・段階分割 |

**合計:** 2 件
(🔴 critical: 1 / 🟠 high: 1)

---

## 1. 🔴 [critical] [test] テスト未整備 14 feature のテスト追加

### 背景

テストのない feature はリグレッションリスクが高い。
nightly / CI で守れない領域が残る。
`/test-design` で観点を整理してから追加するのが最も効率的。
nightly patrol で検知。

### 現状

テストファイルが 0 件の feature ディレクトリが 14 個あり、閾値 (2件) を超えています。

### 対象ファイル

- `src/features/accessibility`
- `src/features/billing`
- `src/features/compliance-checklist`
- `src/features/diagnostics`
- `src/features/kokuhoren-csv`
- `src/features/kokuhoren-preview`
- `src/features/kokuhoren-validation`
- `src/features/official-forms`
- `src/features/operation-hub`
- `src/features/ops-dashboard`
- `src/features/planDeployment`
- `src/features/reports`
- `src/features/resources`
- `src/features/safety`

### 提案

- `src/features/accessibility` (4 files) — `/test-design` で観点整理 → `/test` で追加
- `src/features/billing` (4 files) — `/test-design` で観点整理 → `/test` で追加
- `src/features/compliance-checklist` (4 files) — `/test-design` で観点整理 → `/test` で追加
- `src/features/diagnostics` (8 files) — `/test-design` で観点整理 → `/test` で追加
- `src/features/kokuhoren-csv` (6 files) — `/test-design` で観点整理 → `/test` で追加
- `src/features/kokuhoren-preview` (1 files) — `/test-design` で観点整理 → `/test` で追加
- `src/features/kokuhoren-validation` (5 files) — `/test-design` で観点整理 → `/test` で追加
- `src/features/official-forms` (4 files) — `/test-design` で観点整理 → `/test` で追加
- `src/features/operation-hub` (3 files) — `/test-design` で観点整理 → `/test` で追加
- `src/features/ops-dashboard` (8 files) — `/test-design` で観点整理 → `/test` で追加
- `src/features/planDeployment` (2 files) — `/test-design` で観点整理 → `/test` で追加
- `src/features/reports` (5 files) — `/test-design` で観点整理 → `/test` で追加
- `src/features/resources` (15 files) — `/test-design` で観点整理 → `/test` で追加
- `src/features/safety` (14 files) — `/test-design` で観点整理 → `/test` で追加

### 受入条件

- [ ] 各 feature に最低 1 つのテストファイルがあること
- [ ] テストが CI で通ること
- [ ] カバレッジが主要パスをカバーしていること

**Labels:** `testing`, `nightly-patrol`, `tech-debt`, `priority-critical`

---

## 2. 🟠 [high] [refactor] 巨大ファイル 6 件を監視・段階分割

### 背景

個別には即分割レベルではないが、件数が積み上がっている。
次回の変更時に分割を検討するべき候補。
nightly patrol で検知。

### 現状

600行以上のファイルが 6 件あり、監視閾値 (3件) を超えています。

### 対象ファイル

- `src/features/monitoring/domain/monitoringDailyAnalytics.ts`
- `src/pages/UserDetailPage.tsx`
- `src/features/exceptions/components/ExceptionTable.tsx`
- `src/debug/HydrationHud.tsx`
- `src/features/monitoring/components/MonitoringDailyDashboard.tsx`
- `src/features/planning-sheet/tokuseiBridgeBuilders.ts`

### 提案

- 次回変更が入るタイミングで、対象ファイルの hook 抽出を検討
- 800行を超える前に予防的に分割
- `/refactor` ワークフローで段階的に対応

### 受入条件

- [ ] 各ファイルが次回変更時に 500 行未満を目標にすること
- [ ] 既存テストがすべて通ること

**Labels:** `refactor`, `nightly-patrol`, `tech-debt`, `priority-high`

---

## 使い方

1. 上記の Draft を確認し、対応が必要なものを選ぶ
2. GitHub Issue として起票する（タイトル・本文をコピー）
3. 対応するワークフロー (`/refactor`, `/review`, `/test-design`) を実行

> ⚠️ この Draft は自動生成です。内容を確認してから起票してください。

---

*Generated by Nightly Patrol (Phase B) — Issue Draft Generator*