# LIVE-SCHEMA-MUTATION-V1 captures

Live preflight dumps and schema snapshots stay **local / gitignored**.

Commit only this README and `.gitignore`. Never commit cookies, tokens, or item payloads beyond Title/Id metadata needed for duplicate counts.
