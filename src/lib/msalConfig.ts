// Deprecated shim – keep old import paths working until cleanup is complete.
// Prefer importing from "src/auth/msalConfig" directly.

export * from "../auth/msalConfig";
export { msalConfig as default } from "../auth/msalConfig";
