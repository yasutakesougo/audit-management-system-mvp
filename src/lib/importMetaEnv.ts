
// Deprecated: Use getEnv, readString, readBool from './env' instead.
import { getEnv, readString, readBool } from './env';

// Re-export for compatibility (if needed)
export { getEnv, readString, readBool };
