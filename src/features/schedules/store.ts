import { useSchedules } from './hooks/useSchedules';
import { useSchedulesToday } from './hooks/useSchedulesToday';

export const useSchedulesStore = useSchedules;
export { useSchedules, useSchedulesToday };
