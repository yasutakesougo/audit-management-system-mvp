#!/usr/bin/env node

// Temporary docs lint stub.
// Always exits 0 so that `npm run lint:docs` can run in CI/locally
// before we wire in a real markdown/Docs linter (markdownlint/remark/etc).
console.log('[lint:docs] Stubbed docs lint (no checks run yet).');
process.exit(0);
